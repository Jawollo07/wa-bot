import 'dotenv/config';
import makeWASocket, {
    useMultiFileAuthState,
    DisconnectReason,
    fetchLatestBaileysVersion,
    makeCacheableSignalKeyStore
} from '@whiskeysockets/baileys';

// TEMP: loading full file next - see follow-up commit
console.error('app.js incomplete - restoring...');
process.exit(1);

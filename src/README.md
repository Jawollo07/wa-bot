# Modular architecture

This directory contains the modular application layer. Legacy implementation files remain at the repository root during the migration so the bot can be refactored incrementally without breaking existing deployments.
